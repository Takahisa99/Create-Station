
<div class="main-visual">
  <div class="main-visual-content">
    <p class="main-visual-text">
          Welcome to Nagano Cake!<br>
          This site is an EC site for cake sales.<br>
          Everybody can see products,<br>
          but need a registratin for purchase.
        </p>
    </div>
</div>

<div class="container">
  <div class="row mt-3 mb-3">
    <h1><strong>Create-Station</strong></h1>
  </div>

  <div class="row">
    <% @creates.each do |create| %>
      <div class="col-4">
        <div class="card">
          <%= link_to public_creates_path(create), class:"card-body" do %>
            <%#= image_tag item.get_image(250,250), class:"card-img-top" %><br>
          <% end %>
          <div class="card-body">
            <strong><%= create.name %></strong><br>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <div class="row">
    <div class="col-4 row mt-3 mb-3">
      <p><%= link_to "全ての商品を見る>", public_creates_path %></p>
    </div>
  </div>