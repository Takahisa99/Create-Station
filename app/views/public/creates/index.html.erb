

<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
       <h3><strong>作品一覧</strong> (全 <%= @creates.count %> 件)</h3><br>
    </div>
  </div>
  <% if user_signed_in? %>
  <%= link_to "ユーザー一覧", public_users_path, class: "btn btn-secondary" %>
  <% end %>
  <br><br>
  <div class="container">
        <div class="row">
            <div class="col-sm">
              <%= render 'admin/genres/tag-search' %>
            </div>
            <div class="col-sm">
                <% if user_signed_in? %>
                <%= render 'public/searches/rank'  %>
               <% end %>
            </div>
        </div>
  </div><br><br>

   <%= render 'public/creates/index', creates: @creates %>

  <div class="row">
    <div class="col-12 d-flex justify-content-center  mt-5 ">

    </div>
  </div><br>