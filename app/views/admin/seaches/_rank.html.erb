<%= form_with model: create, url: public_searches_path, method: :get, local: true do |f| %>
    <%= f.select :word, [ ['投稿が新しい順', 'new'],['投稿が古い順', 'old'],['いいねが多い順', 'likes'],['いいねが少ない順', 'dislikes'],] %>
    <%= f.submit "選択する" %>
<% end %>


    <%#= create.each do |create| %>
        <%#= link_to create.user.email, user_path(create.user.id) %>
         <br>
        <%#= link_to create.name, post_path(create.id) %>
         <br>
         <%#= create.created_at %>
         <br>

              <%#= if create.favorited_by?(current_user) %>
                <%#= link_to "いいね: #{create.favorites.count}", create_favorites_path(create.id), method: :delete %>
              <%# else %>
                  <%#= link_to "いいね: #{create.favorites.count}", post_favorites_path(create.id), method: :post %>
              <%# else %>
              <%#= create.favorites.count %>
              <%# end %>

   <%# end %>
